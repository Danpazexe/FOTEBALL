import {coresClaras, esporteClaro} from '../tokens/colors';
import type {TemaDS} from './types';

/** Tema claro — padrão do produto ("Sala de Análise"). */
export const temaClaroDS: TemaDS = {
  esquema: 'claro',
  cores: coresClaras,
  esporte: esporteClaro,
};
