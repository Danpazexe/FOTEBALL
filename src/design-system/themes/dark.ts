import {coresEscuras, esporteEscuro} from '../tokens/colors';
import type {TemaDS} from './types';

/** Tema escuro — alternativa completa. */
export const temaEscuroDS: TemaDS = {
  esquema: 'escuro',
  cores: coresEscuras,
  esporte: esporteEscuro,
};
